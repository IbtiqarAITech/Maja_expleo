# ENSO assets locaux

Source prévue: https://ddp.csum.umontpellier.fr/rob1e/photos

Commande:
```bash
python scripts/fetch_enso_images.py --output-dir docs/assets/enso --limit 10
```

La tentative exécutée dans cet environnement a été bloquée par le proxy HTTP (`Tunnel connection failed: 403 Forbidden`). Le manifeste local `manifest.json` conserve le résultat. Vérifier la licence et l'attribution avant toute diffusion externe.
